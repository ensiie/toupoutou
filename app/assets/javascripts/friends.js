$(document).ready(function () { 

    $(".input_invit").focus();
    
    // Accès tableau de bord
    $(".input_invit").bind('keydown', 'return', function () {
        alert(' Ajout reussi ');
    });
    
})
